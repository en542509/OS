###############################################################################
# test if multiple threads are running
# multiple output of threads should be visible
#############
  std                  # enable debug
  loadi 0xffff, r0
  cld
loop:
  dec r0
  test r0, r0
  br loop

  std
  xor r0, r0
  cld

.literal 0
